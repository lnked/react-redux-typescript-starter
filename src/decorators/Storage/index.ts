export const Storage = ({ ...props }) => {
  console.log(props);

  // descriptor.value = () => {
  //   // console.storage(`Calling "${name}" with`, arguments);
  //   return value.apply(null, arguments);
  // };

  // return descriptor;
  return null;
};
